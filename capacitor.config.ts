import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'price-comparing-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
